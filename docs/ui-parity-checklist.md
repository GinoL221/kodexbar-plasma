# UI parity checklist

Manual craft checklist for the KodexBar Plasma popup and compact representation. Use this with [live-plasma-smoke.md](live-plasma-smoke.md) after layout or polish changes.

Deterministic offscreen selected-provider pixel checks are documented in [visual-regression.md](visual-regression.md); they supplement, but do not replace, this manual sign-off.

**Authority:** structure and information hierarchy may follow CodexBar (Linux/macOS) as a **wireframe**. Styling must remain Plasma 6 + Kirigami + Breeze (`Kirigami.Units`, `Kirigami.Theme`). Do not copy macOS glass, fixed brand blues, or web dashboard cards.

Load `skills/plasma-kirigami-ui/SKILL.md` before implementing UI changes.

## Compact representation

- [ ] Panel control shows usage summary or phase text without layout thrash
- [ ] Compact mark is theme-adaptive (no black/white square under Light/Dark)
- [ ] Click / keyboard activation toggles the popup

## Provider tabs

- [ ] `All` plus usable providers in CLI response order
- [ ] Tab content is **icon + short provider name** only (no email, no `· source` in the label)
- [ ] Full `source` remains available via accessible name/description
- [ ] Horizontal overflow scrolls; no clipped tab row without affordance
- [ ] Keyboard: Tab into bar, arrows/Home/End as native, activation matches selection

## Selected-provider header

- [ ] Provider display name is prominent
- [ ] `Updated …` appears when `usage.updatedAt` is valid; omitted when absent
- [ ] Plan / `loginMethod` shown when valid (no empty placeholder)
- [ ] Account **email** shown only when CLI supplies a valid email
- [ ] **Organization** shown only when human-readable (omit UUID / long hex / opaque tokens)
- [ ] Header enrichment is **absent** in `All` summary rows

## Usage windows

- [ ] Session / Weekly / Monthly rows only when the CLI supplies that window
- [ ] Finite `usedPercent` shows bar + used text; non-finite does not invent a bar
- [ ] Reset text prefers CLI `resetDescription` / raw reset fields; no fabricated durations in QML
- [ ] Dense, readable layout in both Breeze Light and Breeze Dark

## Pace

- [ ] Valid `pace.primary|secondary|tertiary` attaches to the matching window only
- [ ] Missing or malformed pace is omitted (no empty pace row)

## Credits and reset credits

- [ ] Finite non-negative `credits.remaining` may show when present
- [ ] Reset-credit block is **hidden** when `availableCount` is 0 or invalid
- [ ] When `availableCount` > 0: count visible; expirations behind keyboard-reachable disclosure
- [ ] No Redeem / mutate / account action controls

## Cost (selected provider only)

- [ ] Cost section only for selected `codex` or `claude` when CLI cost snapshot is valid
- [ ] Never shown for `All` or unsupported providers
- [ ] Values match CLI fields only (`sessionCostUSD`, tokens, last-30-day fields); no QML pricing
- [ ] Local-estimate labeling / `source: local` intent preserved (not billed-as-subscription copy)
- [ ] Cost failure, timeout, or empty result **hides** the section and does not disturb Usage

## Details and errors

- [ ] `usage.details[]` remains collapsed by default; expandable by pointer and keyboard
- [ ] Free-form details still reject email/org/pace/credit/cost/token-shaped content in titles/rows
- [ ] Global error summary stays bounded and after provider content

## Footer / chrome

- [ ] Refresh reachable and does not replace Usage snapshot incorrectly on failure
- [ ] Settings via Plasma configure action only (no in-popup Auth / Add Account / Quit)

## Theme and accessibility

- [ ] Breeze Light: text, bars, icons, focus rings readable
- [ ] Breeze Dark: same
- [ ] Focus order matches visual order; disclosures announce expand/collapse
- [ ] No hardcoded colors that break one theme

## Anti-patterns (fail if present)

- [ ] No HTML/CSS layout metaphors, web breakpoints, or emoji-as-icon
- [ ] No glassmorphism / fixed macOS accent styling
- [ ] No second request that blocks or clears Usage when Cost fails

## Sign-off

| Field | Value |
|---|---|
| PR / change | visual-parity-polish (PR6 D1-D30, PR7 goldens) |
| Date | 2026-08-16/17 |
| Breeze Light | pass |
| Breeze Dark | pass |
| Notes | Live `plasmawindowed` smoke (task 8.11) on both themes: Overview single-line bars, custom tab chip strip (icons/underline/arrows, no doubled paint), full-length titles, codex/commandcode/mimo icons legible, detail header shows name+Updated+badge only, CLI-verbatim resets, Cost section correct. Offscreen visual-regression goldens (Phase 7) regenerated and converge 0/180000 in all 4 scenarios; `breeze-light-*` goldens inherit a pre-existing (pre-dates this change) black-background/purple-text rendering defect in the Docker/offscreen theme-injection pipeline — confirmed via the prior committed golden, tracked separately (Engram `backlog/visual-regression-light-theme-injection-bug`), not blocking, does not reflect live Plasma Light rendering (which is correct per the live smoke above). |
| PR / change | usage-threshold-marker (PR 3, D16) |
| Date | 2026-08-17 |
| Breeze Light | automated pass; manual smoke pending |
| Breeze Dark | automated pass; manual smoke pending |
| Notes | Usage windows — brand fill + threshold dot warn/critical only; tabs unchanged. Automated: `VisualCaptureHarness.qml` gained a third `{label:"Monthly", usedPercent:94}` window fixture alongside the existing 42%/13% rows; all 4 `breeze-{light,dark}-cost-{present,absent}` goldens regenerated via `docker build -f ci/visual-regression.Dockerfile` + `UPDATE_GOLDENS=1 ./scripts/run-visual-tests.sh`, then a convergence re-run without `UPDATE_GOLDENS` passed 0/180000 mismatched pixels on all 4 scenarios. Visual inspection of the regenerated goldens confirms the dot renders at the 94% (critical) row and is absent at the 42%/13% (ok) rows in both themes; `breeze-light-*` still shows the pre-existing black-background/purple-text injection defect noted above, unrelated to this change. Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — dot legibility, summary-dot noticeability, Overview/detail agreement, tabs unchanged — is **pending human confirmation** (tasks.md 3.5 intentionally left unchecked; cannot be performed in a sandboxed subagent). |
| PR / change | usage-threshold-marker Revision 1 (D19-D22, icon marker, PR 3 amendment) |
| Date | 2026-08-17 |
| Breeze Light | automated regen done, glyph NOT confirmed by automation; manual smoke pending |
| Breeze Dark | automated regen done, glyph NOT confirmed by automation; manual smoke pending |
| Notes | Revision 1 replaced the plain `Rectangle` dot with a `Kirigami.Icon` (`emblem-warning-symbolic` / `emblem-important-symbolic`). All 4 `breeze-{light,dark}-cost-{present,absent}` goldens were re-regenerated with the same Docker image (`docker run --user "$(id -u):$(id -g)" ... UPDATE_GOLDENS=1 ./scripts/run-visual-tests.sh`), then a convergence re-run without `UPDATE_GOLDENS` passed 0/180000 mismatched pixels on all 4 scenarios (deterministic). **However, direct pixel inspection of the regenerated goldens found the icon glyph does not render at all in this offscreen/Docker capture path** — the 94% Monthly row shows the old dot removed and the accent bar's rounded end, but no visible warning/important glyph. Root cause isolated with a standalone repro: `Kirigami.Icon.source` uses freedesktop icon-theme names resolved via `QIcon::fromTheme()`, and `QT_QPA_PLATFORM=offscreen` in this harness loads no platform-theme plugin, so `QIcon::themeName()` stays empty and the lookup silently draws nothing (confirmed: the same icon fragment renders correctly, in the expected `negativeTextColor` red, only when `QT_QPA_PLATFORMTHEME=kde` is added — the exact env var `docs/visual-regression.md` already documents avoiding here because it broke palette classification headless). This is a genuine automated-coverage gap introduced by the icon-marker revision, not a code regression: the marker's classification logic, geometry, and a11y wiring are still covered by `UsageWindowThresholdHarness.qml` (property-level QML assertions, unaffected by icon-theme resolution). The committed goldens are pixel-stable/deterministic but currently provide **no visual signal for the icon glyph itself**. Consequently the live `plasmawindowed` smoke (tasks.md 3.5b) is now the only path that can confirm the icon actually renders and is legible — it remains **pending human confirmation**, and its scope now also includes verifying warn-vs-critical glyph distinguishability. Maintainers should decide separately whether to accept this as a standing harness limitation or invest in a headless icon-theme fix; that decision was left open rather than made unilaterally here. |
| PR / change | usage-threshold-marker Revision 2 (D23-D26, row-sibling placement, PR 3 amendment round 2) |
| Date | 2026-08-17 |
| Breeze Light | automated regen done, marker placement/geometry pixel-verified via reserved-slot measurement; glyph itself NOT confirmed by automation; manual smoke pending |
| Breeze Dark | automated regen done, marker placement/geometry pixel-verified via reserved-slot measurement; glyph itself NOT confirmed by automation; manual smoke pending |
| Notes | Revision 2 moved the marker fully off the accent bar to a row sibling next to the percent text, with its layout slot always reserved at a fixed width (`Kirigami.Units.iconSizes.small`) so bar track length stays identical whether or not a marker is present (D23-D26). All 4 `breeze-{light,dark}-cost-{present,absent}` goldens were re-regenerated with the same Docker image (`docker run --user "$(id -u):$(id -g)" ... UPDATE_GOLDENS=1 ./scripts/run-visual-tests.sh`), then a convergence re-run without `UPDATE_GOLDENS` passed 0/180000 mismatched pixels on all 4 scenarios (deterministic). Direct pixel inspection of the regenerated `breeze-dark-cost-present` golden confirms the structural change took effect: the accent bar is no longer overlaid by anything, and a run-length pixel scan of the Session/Weekly/13%-Weekly/94%-Monthly bar rows shows all three bar tracks span the identical `x≈0–445` pixel range regardless of the 94% row's critical marker — directly confirming the reserved-slot invariant (bar length is unaffected by marker presence). As expected and consistent with the Revision 1 finding above, **no visible warn/critical icon glyph renders next to the percent text in this offscreen/Docker capture path** — this is the same pre-existing, already-documented `QIcon::fromTheme()` / `QT_QPA_PLATFORM=offscreen` harness limitation (not a new regression, and not specific to on-bar vs. row-sibling placement). `breeze-light-*` goldens continue to show the separately tracked, pre-existing black-background/purple-text injection defect noted above (Engram `backlog/visual-regression-light-theme-injection-bug`), unrelated to this change. Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — row-sibling placement reads correctly off the bar, bar lengths stay visually equal across rows regardless of marker presence, warn/critical glyphs distinguishable, tabs unchanged — is **pending human confirmation** (tasks.md 3.5c intentionally left unchecked; cannot be performed in a sandboxed subagent). |
| PR / change | usage-threshold-marker Revision 3 (D27-D30, custom bundled SVG icons + detail-mode placement flip, PR 3 amendment round 3) |
| Date | 2026-08-17 |
| Breeze Light | automated regen done, marker geometry/position pixel-verified; glyph color not visible in the offscreen capture path (root cause identified, see Notes); **live smoke passed** |
| Breeze Dark | automated regen done, marker geometry/position pixel-verified; glyph color not visible in the offscreen capture path (root cause identified, see Notes); **live smoke passed** |
| Notes | Revision 3 replaced the Breeze/Plasma `emblem-*-symbolic` theme-name icons with two bundled mask SVGs (`contents/icons/threshold-warning.svg`, `contents/icons/threshold-critical.svg`, loaded via `Qt.resolvedUrl(...) + isMask:true`, the same convention already used by provider icons) and flipped the detail-mode marker to render after `bandPercentageLabel` instead of before it, while leaving Overview's before-percent order unchanged (D27-D29). All 4 `breeze-{light,dark}-cost-{present,absent}` goldens were re-regenerated with the same Docker image (`docker run --user "$(id -u):$(id -g)" ... UPDATE_GOLDENS=1 ./scripts/run-visual-tests.sh`), then a convergence re-run without `UPDATE_GOLDENS` passed 0/180000 mismatched pixels on all 4 scenarios (deterministic). **Direct pixel inspection again found no visible glyph in the marker's expected slot next to `94% used`** — but this time the cause is different from, and more specific than, Revision 1/2's `QIcon::fromTheme()` theme-plugin gap: the bundled SVG *does* load and paint (confirmed via an isolated same-size, same-`isMask` reproduction outside the app — the resolved-URL SVG loads with correct `sourceSize`, and a 16×16 `isMask:true` render of `threshold-critical.svg` produces the exact circle-with-exclamation-cutout silhouette at the right alpha shape). The bug is that **the `color:` property override on an `isMask:true` `Kirigami.Icon` is not applied when the source is a local `file:`/resolved-URL SVG under this Docker image's Qt 6.11.1 / Kirigami 6.28.0 in the `QT_QPA_PLATFORM=offscreen` + `QT_QUICK_BACKEND=software` combination** — the icon paints using the SVG's own intrinsic fill (`fill="currentColor"`, which resolves to black by default) instead of the bound `Kirigami.Theme.negativeTextColor`/`neutralTextColor`. Because this visual-regression harness's captured content area background is itself solid opaque black in both theme scenarios (a pre-existing property of this fixture, unrelated to Breeze Light/Dark theming — see the scope-boundary note in `docs/visual-regression.md`), a solid-black-on-solid-black icon is pixel-identical to the background (0 delta), which is why it stays invisible and why the automated comparator still converges at 0/180000. **This narrows, rather than closes, the "harness limitation" gap flagged in Phases 5 and 7**: switching to a bundled resource genuinely fixed the *loading* half of the problem (no more `QIcon::fromTheme()` blindness), but exposed a second, previously-hidden *recoloring* gap specific to this offscreen/software rendering path — not something a live Plasma desktop (real platform theme, real color) would necessarily hit. This is a new finding, not a repeat of the prior one, and is reported here rather than silently accepted. The marker's classification logic, geometry, opacity, and a11y wiring remain fully covered by `UsageWindowThresholdHarness.qml`'s property-level assertions (source string, opacity, color-binding, reserved-slot width), which are unaffected by this rendering-only gap. Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — **done, confirmed by user ("ahora si me gusta como quedo")**: glyph shapes read well and are colored correctly on real hardware-accelerated rendering (unaffected by the offscreen/software-only recolor gap above), centered in Overview's slot, detail marker renders after the percent text, tabs unchanged. This live smoke was the only path that could confirm both the glyph *shape* and its *color*, since the automated suite provides no color signal for `isMask` local-SVG icons in this offscreen path — that automated-coverage gap remains a standing, documented limitation of the visual-regression harness, not a defect in the shipped feature. (tasks.md 3.5d marked `[x]`.) |
| PR / change | usage-threshold-exhausted-level (Phase 1, E1-E4) |
| Date | 2026-08-17 |
| Breeze Light | automated pass; manual smoke pending |
| Breeze Dark | automated pass; manual smoke pending |
| Notes | Added a fourth classification level, `exhausted` (`usedPercent >= 100`), alongside the existing `ok`/`warn`/`critical` three — same `negativeTextColor` red as `critical`, distinguished only by a new bundled octagon+diagonal-slash mask icon (`contents/icons/threshold-exhausted.svg`, already user-approved against rendered previews prior to this change). Strict-TDD RED-first: `tests/UsageThresholdHarness.qml`'s boundary table failed on `EXHAUSTED_AT === 100` before `contents/code/UsageThreshold.js` gained `EXHAUSTED_AT`/`LEVEL_EXHAUSTED` and the reordered `level()` branch (exhausted checked before critical, per E1) — confirmed RED (exit 1) then GREEN (exit 0) after the fix. `tests/UsageWindowThresholdHarness.qml`'s new `usedPercent:100` fixture pair (summary+detail) failed on a `thresholdMarker` color mismatch before `contents/ui/UsageWindowRow.qml`'s `thresholdMarkerComponent` gained its 3-way `source`/`color` ternaries (E2-E3) and the `"Quota exhausted"` a11y branch (E4) — confirmed RED (exit 1) then GREEN (exit 0) after the fix; reserved-slot/fill-independence/cross-mode invariants re-asserted against the new fixture and hold. Full `./scripts/run-qml-tests.sh` passes (all harnesses including the two above). `./scripts/lint-qml.sh` passes after re-pinning the standing `LAYOUT_POSITIONING_EXCEPTIONS` line for `UsageWindowRow.qml`'s `titleRow` `width: root.width` binding, which drifted from line 160 to 166 as a direct result of this change's new ternary branches (consistent with every prior edit to this file, per the standing D12 discipline). **Not automated in this change**: no offscreen visual-regression golden work was in scope for Phase 1 (task list contains no golden-regen task, unlike the archived `usage-threshold-marker` change's Phase 7) — the pre-existing, already-documented `isMask` local-SVG-color gap in the offscreen/Docker capture path (see Revision 3 above) is inherited unchanged and remains the standing reason the exhausted icon's actual on-screen legibility/distinguishability-from-critical cannot be confirmed by automation. Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — exhausted icon reads distinct from critical, color matches critical, tabs unchanged — is **pending human confirmation** (tasks.md 1.9 intentionally left unchecked; requires a live Plasma desktop session, cannot be performed by a sandboxed agent). |
| PR / change | compact-icon-only (Phase 1, tasks 1.1-1.4, 1.6) |
| Date | 2026-08-17 |
| Breeze Light | automated pass; manual smoke pending |
| Breeze Dark | automated pass; manual smoke pending |
| Notes | Removed the visible `PlasmaComponents.Label` (percentage text) from `contents/ui/CompactUsageButton.qml`'s `contentItem` `RowLayout` (F1), leaving only the `Kirigami.Icon`, and enlarged it from `Kirigami.Units.iconSizes.small` (16px) to `Kirigami.Units.iconSizes.smallMedium` (22px, F2) — matching the codebase's existing "user-facing" icon floor used elsewhere for provider tab icons. The now-unused `org.kde.plasma.components as PlasmaComponents` import was also removed. `usageText` and its `Accessible.name`/`Accessible.description` bindings were left untouched (F3) — both were already computed directly from `usageText`, independent of the removed visible `Label`. `Layout.minimumWidth`/`Layout.minimumHeight` needed no textual edit: `minimumWidth` is a reactive binding on `contentItem.implicitWidth + Kirigami.Units.smallSpacing * 2` that recomputes automatically now that the `RowLayout` holds only the icon, and `minimumHeight` was already bound to `Kirigami.Units.iconSizes.smallMedium`, which now matches the icon's own new size exactly. Baseline (task 1.1): `tests/CompactUsageButtonHarness.qml` run standalone before any production edit — exit 0, no assertion failures, confirming none of its existing assertions (`activeFocusOnTab`, `focusPolicy`, `Accessible.name`, `enabled`, click activation via `activationCount`) reference the visible Label. Production edit applied (task 1.2), then the same harness re-run unmodified (task 1.3) — still exit 0, no assertion failures, demonstrating F3 held both before and after. Full `./scripts/run-qml-tests.sh` passes (exit 0, all harnesses including `CompactUsageButtonHarness.qml` run to completion). `./scripts/lint-qml.sh` passes (exit 0, only the pre-existing accepted KDE-translation and layout-positioning exceptions, no new ones from this change). **Not automated in this change**: no offscreen visual-regression golden work was in scope (task list contains no golden-regen task for this phase). Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — icon-only rendering, visual size comparable to neighboring panel/tray icons — is **pending human confirmation** (tasks.md 1.5 intentionally left unchecked; requires a live Plasma desktop session, cannot be performed by a sandboxed agent). |
| PR / change | cost-section-compact-format (Phase 1, tasks 1.1-1.8) |
| Date | 2026-08-17 |
| Breeze Light | automated pass; manual smoke pending |
| Breeze Dark | automated pass; manual smoke pending |
| Notes | Reformatted the Cost section from one dense line per period into a scannable two-column layout: `contents/ui/CostSection.qml`'s `costSessionLabel`/`costLast30DaysLabel` `Label`s became `RowLayout`s (period name left, spacer, value right-aligned, `objectName` preserved on the value label per C3), `formatUsd` now produces a fixed comma-decimal/period-thousands string (e.g. `36,27`; `1.210,98`) instead of period-decimal `.toFixed(2)` (C1), `formatTokens` was replaced by `formatTokensAbbreviated` (K/M/B suffix, exactly one decimal, comma-decimal separator, e.g. `108,2M`/`4,0B`/`1,2K`; values under 1000 stay plain integers) (C2), and a new `costLocalEstimateLabel` caption ("Local token-cost estimate", `disabledTextColor`, small font) now renders as the last child of the root `ColumnLayout`, visible only when `hasSnapshot && snapshot.source === "local"` (C4) — closing the existing `provider-cost-estimate` spec's local-estimate-labeling requirement, which had never had a visible implementation before this change. Strict-TDD RED-first: `tests/ProviderRowHarness.qml`'s cost-format assertions were updated to the new expected strings first (`:612` `"1.5"`→`"1,5"`; `:673-676` recomputed from the real `largeCostRow` fixture values — `sessionCostUSD:10.4552`→`"10,46"`, `sessionTokens:139811000`→`"139,8M"`, `last30DaysCostUSD:245.6`→`"245,60"`, `last30DaysTokens:987654321`→`"987,7M"`, both token values landing in the `M` range since `987654321 < 1e9`, not `B` as an earlier draft of the task list assumed), plus one new `costLocalEstimateLabel` visibility assertion — confirmed RED (`qml: ProviderRowHarness failure: enriched row must show session cost`, exit 1) against the still-unmodified `CostSection.qml`, and independently confirmed via an isolated fixture-accurate probe that none of the four new large-cost substrings matched the pre-change output. Then GREEN after the `CostSection.qml` rewrite: full `./scripts/run-qml-tests.sh` passes (exit 0, `ProviderRowHarness.qml` runs to completion with no assertion failure), `./scripts/lint-qml.sh` passes (exit 0, no new positioning/qualification exceptions needed). The "never scientific notation" guard (`:671-672`, `.indexOf("e+") === -1`) was preserved unchanged and still passes against the new functions. **Not in scope for this phase**: no offscreen visual-regression golden work (task list contains no golden-regen task for this phase) and no CSS/layout gating beyond `hasSnapshot` was changed (the new caption is gated separately, per design's explicit out-of-scope note). Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — two-column layout reads well at popup width, caption appears/disappears correctly with `source`, and the one-decimal K/M/B abbreviation style reads right (flagged in design as the one formatting choice not directly confirmed by a direct user answer) — is **pending human confirmation** (tasks.md 1.7 intentionally left unchecked; requires a live Plasma desktop session, cannot be performed by a sandboxed agent). |
| PR / change | provider-selector-remember-tab (Phase 1, tasks 1.1-1.5, 1.7) |
| Date | 2026-08-17 |
| Breeze Light | automated pass; manual smoke pending |
| Breeze Dark | automated pass; manual smoke pending |
| Notes | **Behavior change worth flagging explicitly: `All`/Overview is now a stable default that never silently switches away.** Previously `contents/ui/ProviderSelector.qml`'s `onPopupOpenChanged` called `_selectDefault()` unconditionally on every popup open, which discarded whatever tab the user had selected before closing, and its non-loading fallback (`_selectFirstOrAll`) jumped straight to the *first provider* rather than `All`. Fixed with two small edits: a new `property bool _hasOpenedBefore: false` (G1) makes `onPopupOpenChanged` call `_selectDefault()` only on the very first popup open of a widget session and `_reconcile()` (already existing, already correct) on every subsequent open; `_selectDefault()` itself was simplified to unconditional `root._selectAll(false)` (G2), removing its old `phase === "loading"` branch entirely so there is no more "pending" state that auto-switches away from `All` once data finishes loading. `_pendingDefault` and its now-dead `if (root._pendingDefault)` branches in `_isAllSelected()`, `_resolveSelectedProvider()`, and `_reconcile()` were deliberately left in place, untouched (G3) — they are permanently inert but low-risk to leave, per design; a future cleanup pass may remove them separately. Net user-visible effect: the very first popup open of a session lands on Overview (even if usage data already finished loading); every later reopen restores whichever tab — Overview or a specific provider — was selected when the popup last closed; and Overview, once selected, never auto-jumps to a provider tab on its own, only via an explicit user pick. Strict-TDD RED-first: `tests/ProviderSelectorHarness.qml` was rewritten first against design.md's traced-assertion table (the `:69-72` "first usable" site now asserts the second-ever open keeps Overview instead of resetting to the first provider; the `:79-82` "settle first usable" auto-switch assertion was removed and replaced with an inline "Overview stays selected once loading finishes" assertion, plus an explicit `_activateIndex(1)` pick reinstated immediately after it so the rest of the script's identity-preservation narrative — fallback/reorder/duplicate-provider handling — still has the non-Overview starting point it depended on before; the `:101-105` "reopen default" site was renamed "reopen preserves Overview" and now asserts Overview survives a close/reopen instead of resetting to the first provider "delta"; and the "Regression: selecting a provider while phase is loading" block gained an explicit `s.tabBar.currentIndex = 0; s._activateIndex(0)` reset before its loading-churn setup per decision G4, since reopening no longer produces a predictable pending-Overview state on its own — the block's actual regression intent, that an explicit pick survives subsequent refresh/phase churn, held unchanged once that precondition was restored). Three new dedicated tests were added in isolated component instances (`sFirstOpen`, `sReopenProvider`, `sOverviewStable`) covering, respectively: first open defaults to Overview even with providers already loaded and phase not `"loading"`; reopen preserves a *specific* provider tab (picked "east" of two, closed, reopened, still "east" without re-picking); and Overview surviving a full `idle → loading → providers-arrive → ready` cycle without ever auto-switching. Run against the still-unmodified production code, the harness failed RED for the right reason on the very first rewritten assertion (`qml: ProviderSelectorHarness failure: reopen after providers load keeps Overview (no reset to first provider)`, exit 1) — proving the old unconditional-`_selectDefault()`-on-every-open bug was still present and being caught. After the production fix, the harness passed GREEN on the first run (exit 0) with no further fixes needed: a full manual trace of the entire sequential script's state machine (`_allSelected`/`_selectedIdentity`/`_hasSelectedIdentity`/`_pendingDefault` at every step) was performed before editing, predicting the exact outcome including a consequence the design's own trace table had NOT enumerated — that once `All` becomes "sticky" (no more auto-fallback to a provider once loading settles), every downstream assertion in the script that depended on an *implicit* auto-pick to reach a non-Overview state would also break unless an *explicit* pick was reinstated at the right point; that explicit `_activateIndex(1)` pick (selecting "later") was added proactively for exactly this reason and confirmed correct against real execution, not guessed. Full `./scripts/run-qml-tests.sh` passes (exit 0, all harnesses including `ProviderSelectorHarness.qml` run to completion). `./scripts/lint-qml.sh` passes (exit 0: 87 accepted KDE-translation warnings, 1 accepted layout-positioning exception — the D12 `LAYOUT_POSITIONING_EXCEPTIONS` pin in `scripts/check-qml-unqualified-baseline.py` is scoped to `UsageWindowRow.qml`'s `titleRow`, a file this change does not touch; verified unaffected rather than assumed, no re-pin needed). **Not in scope for this phase**: no offscreen visual-regression golden work (task list contains no golden-regen task; this change touches selection state only, not rendered pixels) and no persistence beyond the current running widget instance — "remember" here means within the current popup session, not across a full Plasma/plasmoid restart (explicitly out of scope per proposal.md). Manual (gating): Breeze Light + Dark `plasmawindowed` live smoke per `docs/live-plasma-smoke.md` — open fresh (lands on Overview), pick a specific provider tab, close/reopen (same provider tab restored), close while on Overview, reopen (still Overview), let a refresh complete while on Overview (stays on Overview, no auto-jump) — is **pending human confirmation** (tasks.md 1.6 intentionally left unchecked; requires a live Plasma desktop session, cannot be performed by a sandboxed agent). |
